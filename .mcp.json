{
  "mcpServers": {
  "memory": {
    "command": "uv",
    "args": ["tool", "run", "--from", "claudescientist==5.1.3", "claudescientist", "mcp", "memory"],
    "startup_timeout_sec": 30
  },
  "verify": {
    "command": "uv",
    "args": ["tool", "run", "--from", "claudescientist==5.1.3", "claudescientist", "mcp", "verify"],
    "startup_timeout_sec": 30
  },
  "prove": {
    "command": "uv",
    "args": ["tool", "run", "--from", "claudescientist==5.1.3", "claudescientist", "mcp", "prove"],
    "startup_timeout_sec": 30
  },
  "cockpit": {
    "command": "uv",
    "args": ["tool", "run", "--from", "claudescientist==5.1.3", "claudescientist", "mcp", "cockpit"],
    "startup_timeout_sec": 30
  },
  "arxiv": {
    "command": "uv",
    "args": ["tool", "run", "arxiv-mcp-server==0.5.0"],
    "startup_timeout_sec": 60,
    "enabled": false,
    "required": false
  },
  "openalex": {
    "command": "npx",
    "args": ["-y", "openalex-research-mcp@0.5.0"],
    "startup_timeout_sec": 60,
    "enabled": false,
    "required": false
  }
  }
}
