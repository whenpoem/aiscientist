"""Tool implementations for memory_mcp, grouped by domain.

The actual MCP-facing surface is exported from :mod:`memory_mcp.impl`. Add
new logic in the appropriate domain submodule, not in ``impl.py``.
"""
