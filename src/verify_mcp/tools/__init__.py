"""Tool implementations for verify_mcp, grouped by domain.

The actual MCP-facing surface is exported from :mod:`verify_mcp.impl`. Add
new logic in the appropriate domain submodule, not in ``impl.py``.
"""
