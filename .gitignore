# Playwright MCP logs
.playwright-mcp/

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.research-agent/
.pytest_cache/
node_modules/
.venv/

# pytest scratch dirs (some tests pin --basetemp; ignore the locked
# remnants that occasionally survive a hard interrupt on Windows).
.pytest-basetemp/
tmp-pytest-local/

# Local environment configuration written by `claudescientist setup`.
# May contain OPENAI_API_KEY and other secrets — never commit.
.env
.env.local

# Generated research reports may contain private results, held-out-derived
# metrics, or unpublished drafts. Add individual files with `git add -f` when
# you intentionally want to share them.
reports/
